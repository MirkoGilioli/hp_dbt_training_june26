version: 2

models:
  - name: stg_orders
    description: "Staging model for orders data."
    columns:
      - name: order_id
        description: "Primary key for orders."
        data_tests:
          - unique
          - not_null
