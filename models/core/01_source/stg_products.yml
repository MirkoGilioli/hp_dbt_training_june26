version: 2

models:
  - name: stg_products
    description: "Staging model for products data."
    columns:
      - name: product_id
        description: "Primary key for products."
        data_tests:
          - unique
          - not_null
